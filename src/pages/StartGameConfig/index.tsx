import { StartGameCard } from 'components';

const StartGameConfig = () => {
  return (
    <div className="bg-slate-700 w-full h-[100vh] flex justify-center align-center">
      <StartGameCard />
    </div>
  );
};

export default StartGameConfig;
